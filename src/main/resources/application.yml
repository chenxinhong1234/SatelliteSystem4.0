spring.datasource:
    url: jdbc:mysql://localhost/reservoirdispatchsystem?autoReconnect=true&useSSL=false&useUnicode=yes&characterEncoding=UTF-8
    username: root
    password: root123
    driver-class-name: com.mysql.jdbc.Driver

spring.jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate.dialect: org.hibernate.dialect.MySQL5Dialect

logging:
  level:
    root: INFO
    org.springframework.web: INFO
    org.springframework.security: INFO

security:
  user:
    password: password

spring:
  thymeleaf:
    cache: false